describe('', () => {
    describe('', () => {
        beforeEach(() => {

        });

        it('21. The subtotal should be computed correctly after deleting a product', () => {

        });

        it('22. The subtotal should be computed correctly after deleting products', () => {

        });

        it('23. The subtotal should be computed correctly after updating a product\'s quantity', () => {

        });

        it('24. The subtotal should be computed correctly after updating each product\'s quantity', () => {

        });
    });
});