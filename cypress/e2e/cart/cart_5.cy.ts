describe('', () => {
    describe('', () => {
        beforeEach(() => {

        });

        it('25. The total should be computed correctly after deleting a product', () => {

        });

        it('26. The total should be computed correctly after deleting products', () => {

        });

        it('27. The total should be computed correctly after updating a product\'s quantity', () => {

        });

        it('28. The total should be computed correctly after updating each product\'s quantity', () => {

        });

        it('29. The total should be computed correctly after selecting a US country', () => {

        });

        it('30. The total should be computed correctly after selecting not a US country', () => {

        });
    });
});